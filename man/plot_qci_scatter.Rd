% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-scatter.R
\name{plot_qci_scatter}
\alias{plot_qci_scatter}
\title{Scatter plot of Male vs Female QCI by region}
\usage{
plot_qci_scatter(
  data,
  score_var = "qci_score",
  year,
  age = "Age-standardized",
  color_by = NULL,
  point_size = 2,
  show_diagonal = TRUE
)
}
\arguments{
\item{data}{A data.frame with columns: \code{location_name}, \code{qci_score}
(or column specified by \code{score_var}), \code{sex_name}, \code{year}, \code{age_name}.
Should include both Male and Female rows.}

\item{score_var}{Character. Column name for the score.
Default \code{"qci_score"}.}

\item{year}{Integer. Single year to plot. Required.}

\item{age}{Character. Default \code{"Age-standardized"}.}

\item{color_by}{Character. Column name to color points by (e.g.,
\code{"type"}). Default \code{NULL} (no color grouping).}

\item{point_size}{Numeric. Point size. Default \code{2}.}

\item{show_diagonal}{Logical. Show y = x line. Default \code{TRUE}.}
}
\value{
A ggplot2 object.
}
\description{
Creates a scatter plot with Male QCI on x-axis and Female QCI on
y-axis, colored by region or custom grouping. Includes a diagonal
reference line (y = x).
}
\examples{
\dontrun{
result <- qci_pipeline(sample_gbd)
map_data <- merge_location_type(result$wide)
plot_qci_scatter(map_data, year = 2019, color_by = "type")
}
}
