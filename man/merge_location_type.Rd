% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{merge_location_type}
\alias{merge_location_type}
\title{Merge with location type metadata}
\usage{
merge_location_type(data, loc_types = NULL, keep_types = "Country")
}
\arguments{
\item{data}{A data.frame with a \code{location_name} column.}

\item{loc_types}{A data.frame with \code{location_name}, \code{type}, \code{iso3} columns.
Defaults to the bundled \link{location_type} dataset.}

\item{keep_types}{Character vector of types to keep.
Default \code{"Country"}. Use \code{NULL} to keep all types.}
}
\value{
data.frame with \code{type} and \code{iso3} columns added, filtered to
requested types.
}
\description{
Joins data with the location_type dataset and optionally filters to
countries only.
}
\examples{
data(sample_gbd)
result <- qci_pipeline(sample_gbd)
merged <- merge_location_type(result$wide)
}
