% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load.R
\name{qci_load}
\alias{qci_load}
\title{Load GBD CSV data}
\usage{
qci_load(files, ...)
}
\arguments{
\item{files}{Character vector of file paths to GBD CSV exports. Can be
a single file or multiple files to be row-bound.}

\item{...}{Additional arguments passed to \code{\link[data.table:fread]{data.table::fread()}}.}
}
\value{
A data.table with standard GBD columns: measure_id, measure_name,
location_id, location_name, sex_id, sex_name, age_id, age_name,
cause_id, cause_name, metric_id, metric_name, year, val, upper, lower.
}
\description{
Reads one or more GBD CSV export files and combines them into a single
data.table. Handles the row-number column that GBD exports include as
the first unnamed column. Uses \code{\link[data.table:fread]{data.table::fread()}} for speed.
}
\examples{
f <- system.file("extdata", "sample_gbd_data.csv", package = "qci")
gbd <- qci_load(f)
head(gbd)
}
