% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-map.R
\name{plot_qci_map}
\alias{plot_qci_map}
\title{Create a choropleth map of QCI scores}
\usage{
plot_qci_map(
  data,
  fill_var = "qci_score",
  years = NULL,
  sex = "Both",
  age = "Age-standardized",
  n_quantiles = 5,
  colors = c("#a40227", "#f56c42", "#dfbf72", "#a7da68", "#1a9850"),
  title = NULL,
  legend_title = "Age-standardized QCI (\%)"
)
}
\arguments{
\item{data}{A data.frame with at least: \code{iso3}, \code{qci_score} (or a column
specified by \code{fill_var}), and optionally \code{year} for faceting.
Use \code{\link[=merge_location_type]{merge_location_type()}} to add \code{iso3} to pipeline output.}

\item{fill_var}{Character. Column name to map to fill color.
Default \code{"qci_score"}.}

\item{years}{Integer vector of years to display. Default \code{NULL} (all).}

\item{sex}{Character. Sex to filter to. Default \code{"Both"}.}

\item{age}{Character. Age group to filter to.
Default \code{"Age-standardized"}.}

\item{n_quantiles}{Integer. Number of quantile bins for the color scale.
Default \code{5}.}

\item{colors}{Character vector of colors for the quantile bins, from
lowest to highest. Default red-to-green scale.}

\item{title}{Character. Plot title. Default \code{NULL} (auto-generated).}

\item{legend_title}{Character. Legend title.
Default \code{"Age-standardized QCI (\%)"}.}
}
\value{
A ggplot2 object.
}
\description{
Plots QCI scores on a world map using natural earth geometries.
Supports faceting by year and custom color scales with quantile-based
breaks.
}
\examples{
\dontrun{
result <- qci_pipeline(sample_gbd)
map_data <- merge_location_type(result$wide)
plot_qci_map(map_data, years = c(1990, 2019))
}
}
