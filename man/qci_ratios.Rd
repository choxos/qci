% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratios.R
\name{qci_ratios}
\alias{qci_ratios}
\title{Calculate QCI component ratios}
\usage{
qci_ratios(wide_number)
}
\arguments{
\item{wide_number}{A data.table in wide format for the Number metric
(the \code{wide_number} element from \code{\link[=qci_clean]{qci_clean()}}).}
}
\value{
The input data.table with 12 new columns appended:
\code{MIR}, \code{lower_MIR}, \code{upper_MIR},
\code{YLLtoYLD}, \code{lower_YLLtoYLD}, \code{upper_YLLtoYLD},
\code{DALtoPER}, \code{lower_DALtoPER}, \code{upper_DALtoPER},
\code{PERtoINC}, \code{lower_PERtoINC}, \code{upper_PERtoINC}.
}
\description{
Computes the four epidemiological ratios that form the basis of the QCI:
\itemize{
\item \strong{MIR} = Deaths / Incidence (Mortality-to-Incidence Ratio)
\item \strong{YLLtoYLD} = YLLs / YLDs (YLL-to-YLD Ratio)
\item \strong{DALtoPER} = DALYs / Prevalence (DALY-to-Prevalence Ratio)
\item \strong{PERtoINC} = Prevalence / Incidence (Prevalence-to-Incidence Ratio)
}
}
\details{
Each ratio is computed for point estimates (\code{val}), \code{upper}, and \code{lower}
uncertainty bounds. \code{Inf} and \code{NaN} values from division by zero are
replaced with \code{NA}.
}
\examples{
data(sample_gbd)
cleaned <- qci_clean(sample_gbd)
with_ratios <- qci_ratios(cleaned$wide_number)
head(with_ratios[, .(location_name, year, MIR, YLLtoYLD, DALtoPER, PERtoINC)])
}
