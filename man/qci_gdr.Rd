% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdr.R
\name{qci_gdr}
\alias{qci_gdr}
\title{Calculate Gender Disparity Ratio (GDR)}
\usage{
qci_gdr(data, low_threshold = 0.95, high_threshold = 1.05)
}
\arguments{
\item{data}{A data.frame or data.table containing at minimum:
\code{location_name} (or \code{location_id}), \code{year}, \code{sex_name}, \code{age_name},
\code{qci_score}. Typically the \code{data} element from \code{\link[=qci_pca]{qci_pca()}} output,
or the \code{wide} element from \code{\link[=qci_pipeline]{qci_pipeline()}}.}

\item{low_threshold}{Numeric. GDR below this value is classified \code{"low"}.
Default \code{0.95}.}

\item{high_threshold}{Numeric. GDR above this value is classified \code{"high"}.
Default \code{1.05}.}
}
\value{
A data.table with columns: \code{location_id}, \code{location_name}, \code{year},
\code{age_name}, \code{qci_female}, \code{qci_male}, \code{gdr}, \code{gdr_category}.
}
\description{
Computes GDR = Female QCI / Male QCI for each location, year, and
age group. Classifies the ratio into categories based on thresholds.
}
\examples{
data(sample_gbd)
result <- qci_pipeline(sample_gbd)
gdr <- qci_gdr(result$wide)
head(gdr)
}
