% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-distribution.R
\name{plot_qci_distribution}
\alias{plot_qci_distribution}
\title{Plot QCI score distributions by sex}
\usage{
plot_qci_distribution(
  data,
  score_col = "qci_score",
  years = NULL,
  sex = c("Male", "Female"),
  age = "Age-standardized",
  alpha = 0.5,
  colors = c(Male = "skyblue4", Female = "firebrick")
)
}
\arguments{
\item{data}{A data.frame with QCI results. Can be wide format (from
pipeline output) or long format.}

\item{score_col}{Character. Column name containing the score to plot.
Default \code{"qci_score"}.}

\item{years}{Integer vector of 1 or 2 years to compare. Default \code{NULL}
(all years overlaid).}

\item{sex}{Character vector of sex categories.
Default \code{c("Male", "Female")}.}

\item{age}{Character. Age group. Default \code{"Age-standardized"}.}

\item{alpha}{Numeric. Transparency for density fills. Default \code{0.5}.}

\item{colors}{Named character vector.
Default \code{c(Male = "skyblue4", Female = "firebrick")}.}
}
\value{
A ggplot2 object.
}
\description{
Creates density plots comparing Male and Female QCI score distributions.
Supports comparing across two time points.
}
\examples{
data(sample_gbd)
result <- qci_pipeline(sample_gbd)
plot_qci_distribution(result$wide, years = c(1990, 2019))
}
